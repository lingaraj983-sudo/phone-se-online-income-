document.addEventListener("DOMContentLoaded", function () { document.querySelectorAll(".faq-question").forEach(function (question) { question.addEventListener("click", function () { const answer = question.nextElementSibling; if (!answer) return; if (answer.classList.contains("open")) { answer.classList.remove("open"); } else { document.querySelectorAll(".faq-answer.open").forEach(function (item) { item.classList.remove("open"); }); answer.classList.add("open"); } }); }); });